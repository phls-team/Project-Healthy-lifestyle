concept_voleyball <- sc_node_not_relation;

=> nrel_main_idtf:
          [voleyball]
          (*<-lang_en;;*);
          [волейбол]
          (*<-lang_ru;;*);

=> nrel_inclusion: concept_block;
		    throw;
		    after_combination;
		    takeoff_combination;
		    return_combination;
		    wave_combination;
		    exit;
		    build_a_roof;
		    double_touch;
		    diagonal_player;
		    finish_off_the_floor;
		    tweaking;
		    replay;
		    outplayer;
		    bow;
		    race_lumbado;
		    delay;
		    back_line;
		    backstop;
		    protection;
		    concept_zone;
		    interval;
		    control_ball;
		    concept_cross_combination;
		    hook;
		    pace;
		    libero;
		    matchball;
		    meter;
		    morita;
		    on_balance;
		    roll_forward;
		    recline;
		    recoup_the_block;
		    pipe;
		    concept_pace;
		    passer;
		    ledge_block;
		    conflict_zone;
		    reverse_cross_combination;
		    straight_cross_combination;
		    rear_cross_combination;
		    backward_wave_combination;
		    mixed_wave_combination;;
